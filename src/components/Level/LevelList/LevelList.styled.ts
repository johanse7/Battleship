import styled from "styled-components"
import { displays } from "../../../theme/base/Variables"

const LevelListStyled = styled.section`
  ${displays.flexColumn};
  gap: 1rem;
`

export default LevelListStyled
